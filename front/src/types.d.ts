export type TTransaction = {
  id: number;
  amount: number;
  beneficiary: string;
  account: string;
  address: string;
  date: string;
  description: string;
};
export type TColumn = {
  Header: string;
  accessor: string;
};
export type TFormValues = {};
